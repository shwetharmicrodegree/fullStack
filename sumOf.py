# Ask the user to input the first number
num = float(input("Enter the first number: "))

# Ask the user to input the second number
num1 = float(input("Enter the second number: "))

# Add the two numbers
sum = num + num1

# Print the sum
print("The sum of", num, "and", num1, "is:", sum)
